function regionGrowth({ width, height, data }, seed, threshold) {
  const canvas = document.getElementById("canvas");
  canvas.width = width;
  canvas.height = height;

  const context = canvas.getContext("2d");



}